[project]
name = "deepeval-mvp"
version = "0.1.0"
description = "building a demo for evaluating LLM output"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepeval>=3.8.1",
    "ollama>=0.6.1",
]

[tool.uv]
package = true
